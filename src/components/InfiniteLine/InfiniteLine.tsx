import React from "react";
import styles from "./InfiniteLine.module.scss";

type Props = {};

const InfiniteLine = (props: Props) => {
  return <div>InfiniteLine</div>;
};

export default InfiniteLine;
